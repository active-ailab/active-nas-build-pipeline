# NAS 固件发布自动化流水线 — 功能全览

> **项目定位**：嵌入式设备固件（华米 Zepp 智能手表）从 CI 构建到文档交付的一站式 DevOps 自动化系统。  
> **核心理念**：配置驱动 + 分层解耦 + 一键发布。  
> **适用场景**：MHS003 / MHS003S / NXP595（Apollo4）系列芯片平台的固件版本发布。

---

## 一、功能总览图

```
┌──────────────────────────────────────────────────────────┐
│  多入口交互                                               │
│  ┌──────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │ CLI 命令行 │  │  Web 管理后台  │  │ 飞书 Bot 自然语言  │  │
│  └──────────┘  └──────────────┘  └───────────────────┘  │
└──────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
┌───────────────┐ ┌───────────────┐ ┌───────────────┐
│ ① Jenkins触发  │ │ ④ TSCAN扫描   │ │ ⑦ Changelog   │
│   构建 + 监控  │ │   独立流程     │ │   差分对比     │
└───────┬───────┘ └───────────────┘ └───────────────┘
        ▼
┌───────────────┐     ┌───────────────┐     ┌───────────────┐
│ ② 下载产物     │ ──▶ │ ③ DSL 准备     │ ──▶ │ ⑤ NAS 上传     │
│  (Jenkins)    │     │ (解包/重组)    │     │  (WebDAV)     │
└───────────────┘     └───────────────┘     └───────┬───────┘
                                                    ▼
                                            ┌───────────────┐
                                            │ ⑥ DSM 共享链接  │
                                            │   生成 + 回填   │
                                            └───────┬───────┘
                                                    ▼
┌──────────────────────────────────────────────────────────────┐
│  交付层                                                       │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐  │
│  │ ⑧ 本地 Markdown │  │ ⑨ 飞书 Wiki/Docx│  │ ⑩ 飞书消息通知  │  │
│  │   版本文档      │  │   在线协作文档   │  │   + Webhook    │  │
│  └────────────────┘  └────────────────┘  └────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

---

## 二、十大核心功能模块

### 1. Jenkins 构建触发与监控

**入口文件**: `jenkins_trigger_build.py`

- 触发 Jenkins 参数化构建（Release + Debug 双任务**并行触发**）
- 自动检测 CSRF Crumb（适配 Jenkins 安全策略）
- 轮询 queue 状态 → 获取 build number → 轮询构建状态直到 SUCCESS/FAIL
- 异步预触发 Changelog 差分任务（`CMP-JIRA-GIT`）
- 构建 URL 自动写回配置文件（`build_url` 字段）
- 支持 `--run-pipeline` 参数自动衔接主流水线

---

### 2. 产物下载

**来源**: `release_pipeline_run.py`

- 从 Jenkins 下载 artifacts（支持 `include`/`exclude` glob 模式过滤）
- 同时下载完整 consoleText（控制台日志，用于问题排查）
- 支持 `artifacts` 模式拉取完整 `archive.zip`
- 按项目分目录存储：`work/download/<项目名>_<release|debug>/`

---

### 3. DSL 驱动的准备阶段（Prepare）

**配置来源**: `placeholders.default.json` + `placeholders.schema.json`

采用声明式 DSL 定义文件处理流程，支持的操作（op）：

| 操作 | 说明 |
|------|------|
| `find` | 按正则表达式匹配查找文件 |
| `pick` | 精确匹配（exact）或 fallback_regex 选取文件 |
| `extract_tgz` | 解压 `.tgz` 归档文件 |
| `extract_tar` | 解压 `.tar` 归档文件 |
| `extract_if_single_tar` | 若目录内仅有一个 `.tar` 则自动解压 |
| `extract_zip` | 解压 `.zip` 归档文件 |
| `copy_to_payload` | 将处理结果复制到上传载荷目录 |
| `download_jenkins_console` | 下载 Jenkins 控制台日志 |

**标准处理流程示例（固件包）:**

1. 定位顶层 `archive_<项目名>_*.tgz` → 解压
2. 穿透内部 tar 层，提取 `archive_OTA_CLOUD_*.tgz`（OTA 云端包）
3. 提取 `watch@mhs003_ota_sign.zip`（OTA 签名包）
4. 提取 `watch@mhs003.elf`（固件 ELF 调试文件）
5. 提取 `archive_BOOT_*.tgz` → 解压 → `bootloader_ota_*.zip`（Bootloader）
6. 提取 `archive_RECOVERY_*.tgz` → 解压 → `recovery_ota_*.zip`（Recovery）
7. 提取 `sensorhub_ota_*.zip`（传感器 OTA）

所有制品按类型分类组织，自动汇聚到 `work/prepare/upload_payload/` 待上传。

---

### 4. NAS 上传（WebDAV）

**入口文件**: `nas_webdav_upload.py`

- 通过 **WebDAV 协议**将制品上传至 NAS 存储
- 使用 curl 命令行 + netrc 临时凭证文件（密码不暴露在进程参数中）
- 支持 `include_globs` / `exclude_globs` 文件过滤
- **并发上传**（ThreadPoolExecutor），大幅提升速度
- 三种上传类型：
  - **debug** → Monkey 子目录（调试版本）
  - **release** → Release 子目录（正式版本）
  - **tscan** → TSCAN 扫描结果
- 支持 `extra_local_dirs` 附加上传 prepare 阶段的 payload

---

### 5. DSM 共享链接生成

- 通过 **Synology DSM FileStation API** 为上传的目录/文件生成共享链接
- 支持 `nas_share_priority` 策略切换（本地 DSM API / 远端汇总 API）
- 生成的链接自动回填到占位符映射表中，供后续文档替换使用

---

### 6. TSCAN 独立扫描流程

- 独立于主发布流程的 TSCAN 扫描通道
- 支持 Web 界面一键触发 "仅 TSCAN" 操作
- 扫描结果同样上传至 NAS 并生成共享链接

---

### 7. Changelog 差分对比

**说明文档**: `CHANGELOG_INTEGRATION_GUIDE.md`

- 异步触发 Jenkins `CMP-JIRA-GIT` 差分任务
- 支持两种等待策略：
  - **阻塞模式**（`block_until_ready`）：等待结果完成后继续
  - **非阻塞模式**：后台触发 + `post_wait_sec` 窗口期补链
- 自动解析控制台日志提取飞书链接
- 自动复用已触发的构建（通过 `output/changelog_job.json` 记录）
- 差分结果链接自动回填到飞书文档的 `{{REL_CHANGELOG_LINK}}` 占位符

---

### 8. 版本文档自动生成

- 自动生成本地 Markdown 版本文档
- 内容包含：
  - 设备名、版本号、发布阶段
  - CI 编译链接（Jenkins Build URL）
  - 各类制品（OTA 包、Bootloader、Recovery 等）的 NAS 下载链接
- 输出路径：`output/<项目名>/<项目>_<版本>_<时间戳>_版本文档.md`
- 支持占位符替换系统，将 `{{REL_*}}` 自动替换为实际值

---

### 9. 飞书深度集成（亮点功能）

**核心适配器**: `lark_cli_adapter.py`（740 行，封装飞书官方 CLI）

#### 9.1 飞书 CLI 适配能力
- 文档创建：Markdown → 飞书 Docx / Wiki
- 文档更新：支持 `overwrite` 全量覆盖 / `str_replace` 精确替换两种模式
- 文档读取：自动适配 Wiki 节点和 Docx 文档，统一返回 Markdown
- 文档授权：给指定用户添加编辑权限
- Wiki 节点复制：lark-cli + HTTP API fallback 双模式保障
- 消息发送：飞书 IM 机器人消息
- 多维表格写入：`lark_base_upsert_record()`
- **一键发布**：`lark_publish_release_document()` —— 完整流水线（复制模板 → 替换占位符 → 超链接化 → 权限分发 → 消息通知）

#### 9.2 OAuth 自动认证
- 本地 HTTP 服务器（localhost:8000）接收 OAuth 回调
- 自动打开浏览器引导用户授权
- Token 交换 + 自动刷新 + 过期自动重新授权
- Token 自动保存到配置文件

#### 9.3 Wiki 模板复制 + 占位符替换
- 在飞书 Wiki 空间中复制预定义模板节点
- 列出 Docx blocks → 批量替换 `{{REL_*}}` 占位符
- **智能超链接**：自动将文件名文本替换为可点击的 NAS 下载链接（通过 Block API）
- **自动验证**：生成后扫描残留占位符，确保替换完整性

#### 9.4 占位符体系

| 类别 | 示例 | 说明 |
|------|------|------|
| 元信息 | `{{REL_DEVICE_NAME}}`, `{{REL_VERSION}}`, `{{REL_STAGE}}` | 设备名、版本号、阶段 |
| Tag 信息 | `{{REL_APP_TAG}}`, `{{REL_BOOT_TAG}}`, `{{REL_RECOVERY_TAG}}` | 各模块 Git Tag |
| CI 信息 | `{{REL_CI_BUILD_URL}}` | Jenkins 构建 URL |
| 制品链接 | `{{REL_RELEASE_FULL_ARCHIVE}}`, `{{REL_RELEASE_OTA_CLOUD_ARCHIVE}}`, `{{REL_MONKEY_SENSORHUB_OTA_ZIP}}` 等 | NAS 共享链接 |
| 文件名 | `{{REL_RELEASE_OTA_SIGN_ZIP_FILENAME}}` 等 | 制品原始文件名 |
| Changelog | `{{REL_CHANGELOG_LINK}}` | 差分结果链接 |

四种占位符解析模式：
- **`mode: var`** — 从配置 JSON 读取值
- **`mode: const`** — 固定文本
- **`mode: nas_share`** — 从 NAS 共享链接生成
- **`mode: filename_for_share`** — 为链接占位符提供对应文件名

---

### 10. Web 管理后台

**入口文件**: `web_release_server.py`  
**技术栈**: Flask + 原生 HTML/CSS/JS + SSE 实时推送

#### 10.1 Web 界面功能
- 项目下拉选择（自动发现所有 `gqf_*.json` 配置）
- 历史版本自动填充（基于 `.bak` 备份文件智能推荐）
- 表单字段可编辑（设备名、版本号、Tag、分支等）
- **实时 SSE 日志流**：构建过程步骤级进度展示
- TSCAN 子任务独立进度追踪
- 任务隔离（按 user_id 区分，多用户互不影响）

#### 10.2 飞书 Bot 智能交互
- 接收飞书 Bot 消息事件回调
- **LLM 意图识别**：通过 DeepSeek AI 模型解析自然语言 → 操作意图
- 正则兜底匹配（LLM 不可用时自动降级）
- 支持的意图：
  - `build` — 触发固件构建
  - `changelog` — 触发差分对比
  - `tscan` — 触发 TSCAN 扫描
  - `skip_jenkins` — 跳过编译，直接基于已有产物生成文档
- 自动提取设备名、版本号、Tag、分支等参数
- 发送确认卡片 / 结果通知到飞书聊天

#### 10.3 27 个 API 端点
涵盖项目管理、配置管理、任务管理、构建触发、SSE 流、飞书回调、统计数据等全场景。

#### 10.4 任务管理系统
- 创建 / 查看 / 停止 / 移除任务
- 超时自动清理（30 分钟以上的已完成任务）
- 任务持久化（防止服务器重启丢失）

---

## 三、支撑性功能

### 统一凭证管理
**文件**: `credential_resolver.py`

- **三级降级策略**: 环境变量 > 配置文件 > 占位符默认值
- 支持 `${ENV:VARNAME}` 占位符自动解析
- 覆盖凭证：Jenkins 密码/用户名、NAS WebDAV/DSM 凭证、飞书 OAuth 密钥/Token、DeepSeek API Key
- Web 前端自动脱敏展示（隐藏敏感字段）

### 配置继承体系
**文件**: `config_loader.py` + `config_migrate.py`

- 子配置文件通过 `base_config` 继承 `common.json` 公共配置
- 深度合并（`_deep_merge`）：基础配置 + 覆盖配置，循环引用检测
- `config_migrate.py` 自动计算公共交集并生成/更新 `common.json`

### 事件通知（Webhook）
- 飞书 Flow Webhook 通知关键事件
- 支持事件类型：`download_start`、`completed`、`failed`、`on_progress`
- 消息模板支持占位符（如 `${release.device_name}`, `${runtime.duration_sec}`）
- 飞书 Bot 直发通知到触发者

### 自动清理
- 每个项目保留最近 **10 个** bak 备份文件
- 每个项目保留最近 **5 个** 下载版本目录
- 过期任务自动清理

---

## 四、技术架构

```
┌─────────────────────────────────────────────────────────┐
│                    表示层 / 交互层                        │
│     CLI 命令行  │  Web UI  │  飞书 Bot 自然语言          │
├─────────────────────────────────────────────────────────┤
│                    编排层 (Orchestrator)                  │
│  lark_release.py           (CLI 统一入口)                 │
│  release_pipeline_run.py   (主流程编排引擎, ~5900 行)     │
│  web_release_server.py     (Web 后台 + Bot, ~3700 行)     │
│  jenkins_trigger_build.py  (Jenkins 构建触发)             │
├─────────────────────────────────────────────────────────┤
│                    适配器层 / 工具层                       │
│  lark_cli_adapter.py       (飞书 CLI 封装, 740 行)        │
│  nas_webdav_upload.py      (NAS WebDAV 上传)             │
│  config_loader.py          (配置继承加载)                  │
│  credential_resolver.py    (凭证解析)                     │
│  config_migrate.py         (配置迁移)                     │
├─────────────────────────────────────────────────────────┤
│                    外部系统依赖                            │
│  Jenkins │ NAS (WebDAV + DSM API) │ 飞书 Open API        │
│  DeepSeek LLM API                                             │
└─────────────────────────────────────────────────────────┘
```

### 配置模型
两级分层 JSON 配置架构：
- **`common.json`** — 所有项目共享的公共配置（Jenkins / NAS / 飞书 / LLM / Webhook）
- **`gqf_<项目名>.json`** — 每个设备独立配置（设备名、版本号、Tag、分支等），通过 `base_config` 继承 common

---

## 五、项目交互方式

| 方式 | 说明 | 适用场景 |
|------|------|---------|
| **CLI 命令行** | `python lark_release.py run --project xxx --version v1.0` | 脚本化/自动化调度 |
| **Web 管理后台** | 浏览器访问 Flask 服务，表单填写 + 实时进度 | 日常手动发布 |
| **飞书 Bot** | 在飞书群聊中 @机器人 发送自然语言指令 | 移动端快速操作 |

---

## 六、支持的设备清单

### NXP595 (Apollo4) 平台
stuttgart · toulouse · toulouseh · andes · berlin · cheetah · swordfish · swift · monaco · vienna · galaxy

### MHS003 平台
pike · windermere · cologne · geneva · lyon · makalu · matterhorn · rimo · rocky · milan · pamir · rome · seattle · warsaw

### MHS003S 平台
oslo · munich · dublin · atlas

> 每个设备对应一个 `gqf_<设备名>.json` 配置文件，新增设备只需添加配置文件即可自动识别。

---

## 七、项目目录结构

```
NAS - 优化版 开发版/
├── 核心脚本
│   ├── release_pipeline_run.py      # 主流程编排引擎 (~5900 行)
│   ├── web_release_server.py        # Web 管理后台 + 飞书 Bot
│   ├── jenkins_trigger_build.py     # Jenkins 构建触发与监控
│   ├── lark_release.py              # CLI 统一入口
│   ├── lark_cli_adapter.py          # 飞书 CLI Python 封装适配器
│   ├── nas_webdav_upload.py         # NAS WebDAV 上传
│   ├── config_loader.py             # JSON 配置继承加载器
│   ├── credential_resolver.py       # 凭证解析器 (env > config)
│   └── config_migrate.py            # 配置迁移工具 (生成 common.json)
│
├── 配置文件
│   ├── common.json                  # 公共基础配置
│   ├── placeholders.default.json    # DSL 占位符 + 处理流程定义
│   ├── placeholders.schema.json     # DSL JSON Schema
│   └── gqf_*.json                   # 各设备项目配置 (~20 个)
│
├── Web 前端
│   ├── templates/
│   └── static/
│
├── 文档与图表
│   ├── diagrams/                    # Mermaid 架构图/流程图/数据流图
│   ├── 链路方案设计.md
│   ├── FEISHU_AUTO_AUTH_GUIDE.md    # 飞书自动认证指南
│   ├── FEISHU_TEMPLATE_EXAMPLES.md  # 飞书模板示例
│   ├── CHANGELOG_INTEGRATION_GUIDE.md # Changelog 集成指南
│   └── 打包命令.md
│
├── 测试文件
│   ├── test_changelog*.py           # Changelog 功能测试
│   ├── test_llm_intent.py           # LLM 意图识别测试
│   ├── test_cred.py                 # 凭证解析测试
│   └── test_env.py                  # 环境变量测试
│
├── 工具 / 历史
│   ├── tools/render_mermaid.ps1     # Mermaid 图表导出
│   └── deprecated_scripts/          # 已废弃的旧版脚本
│
└── 运行时数据
    ├── work/                        # 下载、准备阶段的中间产物
    ├── output/                      # 版本文档、日志、映射文件
    └── user_tokens/                 # 用户飞书 Token
```

---

## 八、关键设计亮点

1. **配置驱动**：新增一个设备只需添加一份 JSON 配置文件，无需修改任何代码
2. **DSL 解耦**：文件处理逻辑与业务逻辑分离，prepare 阶段完全由 JSON DSL 描述
3. **凭证安全**：敏感信息不落盘在代码中，通过环境变量 + netrc + 脱敏展示多层保护
4. **可观测性**：SSE 实时日志、步骤进度、事件 Webhook、任务持久化
5. **容错设计**：飞书 CLI 失败自动 HTTP API fallback；LLM 失败正则兜底；Token 过期自动刷新
6. **多入口一致**：CLI / Web / 飞书 Bot 三条路径共用同一套核心引擎，行为完全一致
7. **异步并行**：Release + Debug 并行构建、多文件并发上传、Changelog 异步预触发
